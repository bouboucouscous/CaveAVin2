{% extends 'base.html.twig' %}

{% block title %}Hello UtilisateurController!{% endblock %}

{% block body %}
    {% include 'homepageTemplate/background.html.twig' %}
    {% include 'utilisateur/navBarUtilisateur.html.twig' %}
    <div class="container" style="margin-top:10%">
        <div class="row same-height justify-content-center">
            <div class="col-md-6 text-center">
                <a href="{{ path('app_vin_new') }}" class="btn btn-primary">Ajouter une bouteille</a>
            </div>
            <div class="col-md-6 text-center">
                <a href="{{ path('app_cave_index') }}" class="btn btn-primary">Voir ça cave</a>
            </div>
        </div>
    </div>
{% endblock %}
