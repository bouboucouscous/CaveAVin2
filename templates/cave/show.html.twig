{% extends 'base.html.twig' %}

{% block title %}Cave{% endblock %}

{% block body %}
    <h1>Cave</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cave.id }}</td>
            </tr>
            <tr>
                <th>Enter_date</th>
                <td>{{ cave.enterDate ? cave.enterDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Exit_date</th>
                <td>{{ cave.exitDate ? cave.exitDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cave_index') }}">back to list</a>

    <a href="{{ path('app_cave_edit', {'id': cave.id}) }}">edit</a>

    {{ include('cave/_delete_form.html.twig') }}
{% endblock %}
